import { EnvOptionsType } from '../types';
import type Space from './Space';
export interface AddSpeakerType extends EnvOptionsType {
    address: string;
}
export declare function addSpeaker(this: Space, options: AddSpeakerType): Promise<void>;
