import { SPACE_INVITE_ROLES } from '../payloads/constants';
import type Space from './Space';
export interface AcceptPromotionRequestType {
    signalData: any;
    promoteeAddress: string;
    role: SPACE_INVITE_ROLES;
    spaceId: string;
}
export declare function acceptPromotionRequest(this: Space, options: AcceptPromotionRequestType): Promise<void>;
