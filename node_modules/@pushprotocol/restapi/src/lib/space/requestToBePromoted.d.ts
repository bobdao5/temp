import { SPACE_INVITE_ROLES } from '../payloads/constants';
import type Space from './Space';
export interface RequestToBePromotedType {
    role: SPACE_INVITE_ROLES;
    spaceId: string;
    promotorAddress: string;
}
export declare function requestToBePromoted(this: Space, options: RequestToBePromotedType): Promise<void>;
