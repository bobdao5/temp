import { EnvOptionsType, SignerType } from '../types';
export interface StartSpaceType extends EnvOptionsType {
    spaceId: string;
    account?: string;
    signer?: SignerType;
    pgpPrivateKey?: string;
}
import type Space from './Space';
declare type StartType = {
    livepeerApiKey: string;
};
export declare function start(this: Space, options: StartType): Promise<void>;
export {};
