import type { ChatFeedsType } from '../../types';
interface fetchChats {
    page: number;
    chatLimit: number;
}
declare const useFetchChats: () => {
    fetchChats: ({ page, chatLimit }: fetchChats) => Promise<ChatFeedsType | undefined>;
    error: string | undefined;
    loading: boolean;
};
export default useFetchChats;
