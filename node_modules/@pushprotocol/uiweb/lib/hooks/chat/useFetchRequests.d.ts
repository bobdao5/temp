import type { ChatFeedsType } from '../..';
interface fetchRequests {
    page: number;
    requestLimit: number;
}
declare const useFetchRequests: () => {
    fetchRequests: ({ page, requestLimit }: fetchRequests) => Promise<ChatFeedsType | undefined>;
    error: string | undefined;
    loading: boolean;
};
export default useFetchRequests;
