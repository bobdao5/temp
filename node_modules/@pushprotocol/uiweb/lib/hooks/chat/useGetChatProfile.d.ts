import * as PushAPI from '@pushprotocol/restapi';
export interface GetProfileParams {
    profileId: string;
}
declare const useGetChatProfile: () => {
    fetchChatProfile: ({ profileId }: GetProfileParams) => Promise<PushAPI.IUser | undefined>;
};
export default useGetChatProfile;
