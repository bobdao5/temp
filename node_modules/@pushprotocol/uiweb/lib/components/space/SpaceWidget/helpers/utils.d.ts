import { SpaceDTO } from '@pushprotocol/restapi';
export declare const isHostOfSpace: (account: string, spaceData: SpaceDTO) => boolean;
export declare const isMemberOfSpace: (account: string, spaceData: SpaceDTO) => boolean;
export declare const isLiveSpace: (spaceData: SpaceDTO) => boolean;
export declare const isTimeToStart: (spaceData: SpaceDTO, now: Date) => boolean;
