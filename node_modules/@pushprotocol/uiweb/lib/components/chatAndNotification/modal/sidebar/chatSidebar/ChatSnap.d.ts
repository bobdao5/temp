import type { IFeeds } from '@pushprotocol/restapi';
import React from 'react';
declare type ChatSnapPropType = {
    chat: IFeeds;
    id: string;
    modalOpen?: boolean;
};
export declare const ChatSnap: React.FC<ChatSnapPropType>;
export {};
